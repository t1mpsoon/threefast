# Express Pick-Up — зависимости MVP.
#
# ВАЖНО (отклонение от раздела 2.16 ТЗ, помеченного там как [ТРЕБУЕТ ПРОВЕРКИ]):
# 1) Версии не пинованы жёстко: ТЗ прямо просило проверить актуальные
#    совместимые версии для выбранной версии Python (3.11+). Здесь проверено
#    на Python 3.14. Пиновать имеет смысл при развёртывании на конкретной машине.
# 2) `passlib[bcrypt]` заменён на `bcrypt`: passlib 1.7.4 (последний релиз, 2020)
#    обращается к `bcrypt.__about__`, удалённому в bcrypt 4.x, а его
#    pure-python backend несовместим с Python 3.14. Функциональность
#    (bcrypt-хэширование паролей) сохранена полностью, см. app/security.py.
# 3) alembic оставлен в стеке (раздел 2.2), но схема по умолчанию создаётся
#    через `python -m app.init_db` — это разрешено разделом 2.2 ТЗ.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.4
sqlalchemy>=2.0.35
alembic>=1.13
jinja2>=3.1
python-dotenv>=1.0
bcrypt>=4.2
python-jose[cryptography]>=3.3
apscheduler>=3.10
python-multipart>=0.0.9

# Тестирование (не требуется для рантайма)
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
